# =============================================================================
# 3S Engenharia — Dependências Back-end
# =============================================================================

# --- Produção ---
Django==5.2
djangorestframework==3.16.0
django-cors-headers==4.7.0
python-dotenv==1.1.0
psycopg2-binary==2.9.10       # driver PostgreSQL
whitenoise==6.9.0             # serve arquivos estáticos (front-end/dist) em produção
gunicorn==23.0.0              # servidor WSGI para produção
requests==2.31.0              # HTTP client para integração com APIs externas (ViaCEP)

# --- Geração de PDF ---
xhtml2pdf==0.2.17             # converte HTML → PDF (puro Python, usa reportlab)
# weasyprint==62.3            # alternativa com mais recursos (requer libs do sistema)

# --- Qualidade de código ---
ruff==0.9.10                  # linting + formatação (substitui black, flake8, isort)

# --- Testes ---
pytest==8.3.5
pytest-django==4.10.0
responses==0.25.0             # mock de requisições HTTP nos testes
