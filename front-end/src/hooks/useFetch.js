// TODO: implementar hook customizado para encapsular chamadas à API e estado de loading/erro
// Sugestão: abstrair o padrão loading/error/data para qualquer requisição assíncrona

// import { useState } from "react";

// TODO: definir e exportar o hook
// Exemplo de estrutura:
//
// export function useFetch(fetchFn) {
//   const [data, setData] = useState(null);
//   const [loading, setLoading] = useState(false);
//   const [error, setError] = useState(null);
//
//   async function execute(...args) {
//     // TODO: implementar chamada, controle de loading e tratamento de erro
//   }
//
//   return { data, loading, error, execute };
// }
